{
  "name": "sql-sandbox-e2e",
  "private": true,
  "scripts": {
    "test:e2e": "bash -c 'set -a && source .env.e2e && set +a && playwright test'",
    "test:e2e:ui": "bash -c 'set -a && source .env.e2e && set +a && playwright test --ui'",
    "test:e2e:headed": "bash -c 'set -a && source .env.e2e && set +a && playwright test --headed'"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
